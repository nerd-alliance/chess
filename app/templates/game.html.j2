<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
  </head>
  <body>
    <table>
      {% for row in board.rows %}
        <tr>
          {% for col in board.cols %}
            {% set tile = board.get_piece_symbol(col, row) %}
            <td style="background-color: red">{{ tile }}</td>
            <!--TODO add superscript of square coords ("a1", "d6" ect)-->
          {% endfor %}
        </tr>
      {% endfor %}
    </table>
  </body>
</html>
